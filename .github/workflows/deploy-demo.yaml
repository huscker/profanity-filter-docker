name: Deploy (demo)

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  deploy_demo:
    environment: demo
    runs-on: ubuntu-latest
    #    needs:
#      - build_and_push
#      - release_charts
    steps:
      - uses: actions/checkout@v3
      - uses: azure/setup-helm@v4.2.0
        with:
          version: 'v3.15.3'
      - name: "Install helm secrets plugin"
        run: helm plugin install https://github.com/jkroepke/helm-secrets --version v4.6.2
      - run: echo "$KUBECONFIG" | wc -c
        env:
          KUBECONFIG: ${{ secrets.GITHUB_TOKEN}}
      - name: Import GPG key
        uses: crazy-max/ghaction-import-gpg@v6
        env:
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
      - uses: azure/k8s-set-context@v4
        with:
          method: kubeconfig
          kubeconfig: ${{ secrets.KUBECONFIG }}
      - name: "Upgrade demo release"
        run: >-
          helm secrets upgrade
          --wait
          --atomic
          --install
          --timeout=10m
          --namespace=profanity-filter-demo
          --create-namespace
          --values=demo/secrets.yaml
          profanity-filter
          demo



